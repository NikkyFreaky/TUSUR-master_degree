services:
  service_3:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service_3
    env_file:
      - ../.env
    networks:
      - lab3-net

networks:
  lab3-net:
    external: true
