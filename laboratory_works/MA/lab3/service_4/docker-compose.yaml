services:
  service_4:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service_4
    env_file:
      - ../.env
    networks:
      - lab3-net

networks:
  lab3-net:
    external: true
