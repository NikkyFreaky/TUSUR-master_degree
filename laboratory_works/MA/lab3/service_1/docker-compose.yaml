services:
  service_1:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service_1
    env_file:
      - ../.env
    networks:
      - lab3-net

networks:
  lab3-net:
    external: true
